jQuery(document).ready(function ($) {

    // Slick slider configuration options
    jQuery(".your-slider-section").slick({
        dots: true,
        arrows: false,
        infinite: true,
        slidesToShow: 1,
        slidesToScroll: 1,
		// vertical: true,
		// verticalSwiping : true,
    });
});